(ns protean.test.matrixparams
  (:require [protean.api.transformation.coerce :as c]
            [protean.api.transformation.sim :refer :all]))
{
  "gu" {
    "groups${;groupFilter}" {
      :get (fn [request responses]
             (let [mp (matrix-params request "groupFilter")]
               (respond responses 200 (c/js mp))))
    }
  }
}
