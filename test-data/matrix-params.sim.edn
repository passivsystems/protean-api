(ns protean.test.matrixparams
  (:require [protean.api.transformation.sim :refer :all]))
{
  "gu" {
    "groups${;groupFilter}" {
      :get #(if (validate %)
              (respond % 400)
              (respond % 200 (matrix-params % "groupFilter")))
    }
  }
}
