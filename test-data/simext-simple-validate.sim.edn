(ns protean.test.simplesim
  (:require [protean.api.transformation.sim :refer :all]))

{
  "sample" {
    "simple" {
      :get #(if-let [errs (validate %)] (respond % 400 errs) (respond % 200))
    }
    "complex" {
      :get #(if (validate %) (respond % 403) (respond % 200))
    }
  }
}
